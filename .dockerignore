# Keep secrets, training data, and junk OUT of the deployed image.
**/__pycache__
**/*.pyc
**/.pytest_cache

# Secrets — never bake into the image
backend/.env
frontend/.env

# training data / experiments (videos are kept so practice clips still show)
backend/dataset
backend/data/landmarks
backend/experiments
backend/ml_env
backend/instance
backend/configs
backend/scripts
backend/tests
backend/*.csv

# Frontend deps / prebuilt output (image rebuilds these)
frontend/node_modules
frontend/build

# Repo / editor / docs cruft
.git
.github
*.pdf
*.md
